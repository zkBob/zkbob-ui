const UNLOCK_PAGE = '.unlock-page';
const passwordInput = `${UNLOCK_PAGE} #password`;
const unlockButton = `${UNLOCK_PAGE} button`;

export const unlockPageElements = {
  UNLOCK_PAGE,
  passwordInput,
  unlockButton,
};
